# AI Provider API Keys
# NOTE: Any `VITE_` value is embedded into the client bundle and is NOT secret.
# For production, prefer BYOK via the in-app Settings (stored locally in the browser),
# or add a backend proxy that holds secrets server-side.
# These env vars are treated as dev-only by the app to avoid accidental key leaks.
VITE_OPENROUTER_API_KEY=
VITE_GROQ_API_KEY=
VITE_CEREBRAS_API_KEY=
VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=
VITE_GEMINI_API_KEY=
VITE_ZAI_API_KEY=

# PocketBase (for auth and cloud storage)
# Local: http://127.0.0.1:8090
# Production: https://substantifiquedb.rosehilltech.com
VITE_POCKETBASE_URL=http://127.0.0.1:8090

# Data provider (pocketbase | supabase)
VITE_DATA_PROVIDER=pocketbase

# Production App URL (for shareable links)
# In development, leave empty to use localhost
# In production, set to your deployed URL (e.g., https://timeline.example.com)
VITE_APP_URL=
